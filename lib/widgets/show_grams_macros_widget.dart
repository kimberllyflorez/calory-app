import 'package:flutter/material.dart';
class ShowGramsMacroWidget extends StatelessWidget {
  const ShowGramsMacroWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Table(
      children: [
        TableRow(

        ),
        TableRow()
      ],
    );
  }
}
